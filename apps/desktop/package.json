{
  "name": "collab-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Collab MVP — application desktop Tauri (Windows/Mac/Linux). Backend cloud-first (VITE_API_URL).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:cargo": "cd src-tauri && cargo build --release --features custom-protocol",
    "icons": "tauri icon ../../landing_preview.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3"
  }
}
