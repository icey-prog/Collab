# URL du serveur backend (API + WebSocket)
# Pointe vers ton VPS ou ton domaine de prod.
# En dev local : laisser vide (Vite proxifie /api → :3001)
VITE_API_URL=https://collab.monserveur.com

# URL publique du frontend (utilisée dans les QR codes partagés).
# Si frontend et backend sont sur le même domaine, laisser vide.
# VITE_PUBLIC_URL=https://collab.monserveur.com
